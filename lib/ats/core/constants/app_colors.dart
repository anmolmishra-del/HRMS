import 'package:flutter/material.dart';

class AppColors {
  // PRIMARY COLORS - Mapped to HRMS Brand Colors
  static const Color primary = Color(0xFF3B82F6);      // Bright Blue from HRMS
  static const Color secondary = Color(0xFF1E3A8A);    // Indigo from HRMS
  static const Color lightBlue = Color(0xFF60A5FA);
  static const Color darkBlue = Color(0xFF1E3A8A);

  static const Color indigo = Color(0xFF1E3A8A);       // Indigo from HRMS
  static const Color brightBlue = Color(0xFF3B82F6);   // Bright Blue from HRMS

  // BACKGROUND
  static const Color background = Color(0xFFF8FAFC);
  static const Color card = Colors.white;

  // TEXT COLORS
  static const Color textPrimary = Color(0xFF111827);
  static const Color textSecondary = Color(0xFF6B7280);
  static const Color textWhite = Colors.white;

  // STATUS COLORS
  static const Color success = Color(0xFF10B981);
  static const Color warning = Color(0xFFF59E0B);
  static const Color error = Color(0xFFEF4444);

  // BORDER & SHADOW
  static const Color border = Color(0xFFE5E7EB);
  static const Color shadow = Color(0x1A000000);
}
